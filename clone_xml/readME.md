# Store clone detection results here
## The source files of all versions should be downloaded to the local for code clones detection, and the .cc suffix of Apollo is changed to .cpp suffix when detecting.
